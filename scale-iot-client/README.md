In order to create the environment, you need to have a running Ceph cluster.
To create the environment for running the Scale IoT code, execute
the following commands.
```shell
cd ceph-ansible
vagrant ssh client0
cd /vagrant/scale-iot-client
bash client.sh
```
